export const CHART_AXIS_CLS = 'text-[10px] fill-muted-foreground font-medium'

export const CHART_TOOLTIP_STYLE = {
  background: 'hsl(var(--background))',
  border: '1px solid hsl(var(--border))',
  borderRadius: 'var(--radius)',
} as const
